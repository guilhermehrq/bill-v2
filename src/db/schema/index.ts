// Domain schema lives here (Phase 1). See prompt §4.
// Each entity (accounts, transactions, categories, ...) will export from its own file,
// and this index will re-export the aggregate.
export {};
