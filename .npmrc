# Use flat node_modules (like npm) so the same install works from
# both WSL and Windows. pnpm's default symlinked store lives in
# /home/<user>/.pnpm-store which Windows Node can't resolve.
node-linker=hoisted
