# App
NEXT_PUBLIC_APP_URL=http://localhost:3000   # use canonical origin in prod (for OAuth redirects)

# Supabase (ADR 002)
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
SUPABASE_SERVICE_ROLE_KEY=

# Postgres connection strings (Supabase provides both in project settings)
DATABASE_URL=       # pooled connection — used at runtime
DIRECT_URL=         # direct connection — used by drizzle-kit migrations

# Observability (Phase 1+)
SENTRY_DSN=

# Rate limiting (Phase 6+)
UPSTASH_REDIS_REST_URL=
UPSTASH_REDIS_REST_TOKEN=
